import { PageLayout } from "@/components/PageLayout";
import { ImageSizeConverter } from "@/components/tools/ImageSizeConverter";

const ImageSizeConverterPage = () => {
  return (
    <PageLayout>
      <ImageSizeConverter />
    </PageLayout>
  );
};

export default ImageSizeConverterPage;